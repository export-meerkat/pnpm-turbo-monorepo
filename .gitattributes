# 1. 기본 텍스트 파일은 저장소 적재 시 강제로 LF(리눅스 표준) 통일
* text=auto eol=lf

# 2. 셸 스크립트 등 운영체제 실행 파일의 줄바꿈 락 (CRLF 유입 시 리눅스 서버에서 실행 에러 유발)
*.sh text eol=lf

# 3. 이미지 및 PDF 등 바이너리 아티팩트 자산의 강제 텍스트 연산 배제 (파일 오염 방지)
*.png *.jpg *.jpeg *.gif *.ico *.pdf binary
